# ── Dependencies ──
node_modules

# ── Build outputs ──
dist
dist-ssr

# ── Logs ──
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Local env ──
*.local
.env
.env.*
!.env.example

# ── Editor ──
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Android: local-only files (android/ folder KEPT in git for CI/CD) ──
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/capacitor-cordova-android-plugins/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
android/app/src/main/res/xml/config.xml

# ── iOS: local-only (ios/ folder KEPT in git for CI/CD) ──
ios/App/Pods/
ios/App/DerivedData/
ios/.idea/

# ── Keystore: NEVER commit private keystore to git ──
*.jks
*.keystore
keystore/
